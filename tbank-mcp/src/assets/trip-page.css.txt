:root {
  color-scheme: light;
  --ink: #161616;
  --muted: #6d6d6d;
  --paper: #f5f5f2;
  --card: #ffffff;
  --accent: #ffdd2d;
  --line: #deded8;
  --purple: #7b61ff;
  --orange: #ff7a00;
  --pink: #ec2b67;
  font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  font-synthesis: none;
}
* { box-sizing: border-box; }
html { scroll-behavior: smooth; background: var(--paper); }
body { margin: 0; color: var(--ink); background: var(--paper); line-height: 1.5; }
a { color: inherit; }
.hero { background: var(--accent); padding: 24px max(24px, calc((100vw - 1180px) / 2)) 68px; }
nav { display: flex; justify-content: space-between; align-items: center; font-size: 14px; }
.brand { font-size: 20px; font-weight: 850; letter-spacing: -.04em; }
.hero-grid { display: grid; grid-template-columns: 1fr 280px; gap: 64px; align-items: end; margin-top: 88px; }
h1, h2, h3, h4, p { margin-top: 0; }
h1 { max-width: 820px; margin-bottom: 18px; font-size: clamp(48px, 8vw, 104px); line-height: .92; letter-spacing: -.065em; }
.hero p { max-width: 720px; font-size: 20px; }
.hero aside { display: flex; flex-direction: column; gap: 6px; padding: 24px; border: 1px solid rgba(0,0,0,.25); border-radius: 24px; }
.hero aside strong { font-size: 30px; line-height: 1.1; }
.hero aside small { margin-top: 16px; }
main { width: min(1180px, calc(100% - 48px)); margin: 0 auto; }
section { padding: 88px 0; border-bottom: 1px solid var(--line); }
.section-head { display: grid; grid-template-columns: 1fr minmax(260px, 420px); gap: 48px; align-items: end; margin-bottom: 34px; }
.section-head > div { display: flex; gap: 18px; align-items: baseline; }
.section-head span { color: var(--muted); font-variant-numeric: tabular-nums; }
h2 { margin: 0; font-size: clamp(32px, 5vw, 56px); line-height: 1; letter-spacing: -.045em; }
.section-head p { margin: 0; color: var(--muted); }
.eyebrow { margin-bottom: 14px; color: var(--muted); font-size: 12px; font-weight: 800; letter-spacing: .09em; text-transform: uppercase; }
.budget-grid, .transport-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
.transport-grid { grid-template-columns: repeat(2, 1fr); }
.budget-card, .transport-card, .hotel-card, .event-card, .venue-card, .plan { background: var(--card); border: 1px solid var(--line); border-radius: 24px; overflow: hidden; }
.budget-card, .transport-card { padding: 26px; }
.price { margin: 14px 0 8px; font-size: 28px; font-weight: 850; letter-spacing: -.04em; }
.range, .budget-card p { color: var(--muted); font-size: 14px; }
.pill { display: inline-block; padding: 6px 10px; color: var(--ink); background: var(--paper); border-radius: 999px; font-size: 11px; }
.route { display: grid; grid-template-columns: 1fr auto 1fr; gap: 16px; align-items: center; font-size: 24px; font-weight: 800; }
.route span:last-child { text-align: right; }
.route b { display: grid; place-items: center; width: 44px; height: 44px; background: var(--accent); border-radius: 50%; }
.times { display: flex; justify-content: space-between; margin-top: 22px; padding-top: 18px; border-top: 1px solid var(--line); }
.button { display: inline-flex; margin-top: 20px; padding: 11px 16px; background: var(--ink); color: #fff; border-radius: 12px; text-decoration: none; font-size: 14px; font-weight: 700; }
.button:hover { transform: translateY(-1px); }
.card-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; }
.card-grid.hotels { grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }
.hotel-card.selected { outline: 3px solid var(--accent); outline-offset: -3px; }
.card-body { padding: 24px; }
.card-body h3 { margin-bottom: 8px; font-size: 24px; line-height: 1.15; letter-spacing: -.025em; }
.card-body p { color: var(--muted); }
.image-frame { position: relative; aspect-ratio: 16/10; overflow: hidden; background: #e7e7e2; }
.image-frame img { width: 100%; height: 100%; object-fit: cover; display: block; }
.image-fallback { display: none; width: 100%; height: 100%; place-items: center; padding: 24px; color: var(--muted); text-align: center; }
.image-frame.failed img { display: none; }
.image-frame.failed .image-fallback, .hotel-card > .image-fallback { display: grid; aspect-ratio: 16/10; }
.credit { position: absolute; right: 8px; bottom: 8px; max-width: calc(100% - 16px); padding: 4px 7px; background: rgba(0,0,0,.72); color: white; border-radius: 6px; }
.rating { display: flex; align-items: baseline; gap: 5px; margin: 16px 0; }
.rating b { display: inline-grid; place-items: center; min-width: 48px; height: 40px; padding: 0 10px; background: var(--accent); border-radius: 12px; font-size: 20px; }
.rating span { color: var(--muted); }
#trip-map { position: relative; min-height: 520px; z-index: 0; background: #e6e6df; border: 1px solid var(--line); border-radius: 28px; overflow: hidden; }
#trip-map .map-fallback { position: absolute; inset: 0; display: none; place-items: center; z-index: 500; background: #e6e6df; color: var(--muted); }
#trip-map.failed .map-fallback { display: grid; }
#trip-map.tiles-failed { background: repeating-linear-gradient(45deg,#ecece7,#ecece7 12px,#e3e3dd 12px,#e3e3dd 24px); }
.leaflet-popup-content a { font-weight: 750; text-decoration: none; }
.map-legend { display: flex; flex-wrap: wrap; gap: 18px; margin-top: 16px; font-size: 13px; }
.map-legend span::before { content: ""; display: inline-block; width: 10px; height: 10px; margin-right: 7px; border-radius: 50%; background: var(--ink); }
.map-legend .event::before { background: var(--purple); }.map-legend .restaurant::before { background: var(--orange); }.map-legend .bar::before { background: var(--pink); }
.plans { display: grid; gap: 12px; }
.plan summary { display: grid; grid-template-columns: 56px 1fr; gap: 18px; padding: 24px; cursor: pointer; list-style: none; }
.plan summary::-webkit-details-marker { display: none; }
.plan summary h3 { margin-bottom: 5px; font-size: 24px; }
.plan summary p { margin: 0; color: var(--muted); }
.plan-number { display: grid; place-items: center; width: 48px; height: 48px; background: var(--accent); border-radius: 50%; font-weight: 850; }
.plan-content { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px; padding: 0 24px 28px 98px; }
.plan-day h4 { font-size: 17px; }
.plan-day ol { list-style: none; margin: 0; padding: 0; }
.plan-day li { display: grid; grid-template-columns: 96px 1fr; gap: 12px; padding: 13px 0; border-top: 1px solid var(--line); }
.plan-day time { color: var(--muted); font-variant-numeric: tabular-nums; }
.plan-day a { font-weight: 750; text-decoration-thickness: 1px; text-underline-offset: 3px; }
.plan-day p { margin: 4px 0 0; color: var(--muted); font-size: 14px; }
.fine-print { display: grid; grid-template-columns: 1fr 1fr; gap: 48px; }
.fine-print h2 { font-size: 28px; }
.sources { list-style: none; padding: 0; }
.sources li { display: flex; justify-content: space-between; gap: 24px; padding: 10px 0; border-bottom: 1px solid var(--line); }
.source-link { font-weight: 700; }
.sources small { color: var(--muted); white-space: nowrap; }
footer { display: flex; justify-content: space-between; gap: 24px; padding: 32px max(24px, calc((100vw - 1180px) / 2)); background: var(--ink); color: white; }
.empty { padding: 40px; background: white; border-radius: 24px; color: var(--muted); }
@media (max-width: 900px) {
  .hero-grid, .section-head, .fine-print { grid-template-columns: 1fr; }
  .hero-grid { gap: 28px; margin-top: 58px; }
  .budget-grid { grid-template-columns: repeat(2, 1fr); }
  .card-grid { grid-template-columns: repeat(2, 1fr); }
  .section-head { gap: 14px; }
}
@media (max-width: 620px) {
  main { width: min(100% - 28px, 1180px); }
  .hero { padding-inline: 18px; }
  h1 { font-size: 48px; }
  section { padding: 58px 0; }
  .budget-grid, .transport-grid, .card-grid { grid-template-columns: 1fr; }
  .plan-content { padding-left: 24px; }
  #trip-map { min-height: 420px; }
  footer { flex-direction: column; }
}
@media print {
  .hero { padding: 24px; }
  .hero-grid { margin-top: 30px; }
  main { width: 100%; padding: 0 20px; }
  section { break-inside: avoid; padding: 32px 0; }
  .button { border: 1px solid var(--ink); color: var(--ink); background: transparent; }
  #trip-map { min-height: 320px; }
}
