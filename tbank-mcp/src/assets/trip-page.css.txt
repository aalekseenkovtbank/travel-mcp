:root {
  color-scheme: light;
  --ink: #172520;
  --muted: #6f786f;
  --cream: #f5f1e8;
  --paper: #fffdf8;
  --line: #ded9ce;
  --orange: #ff5938;
  --green: #174d3e;
  --gold: #d89e2b;
  --purple: #7557d9;
  font-family: "DM Sans", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  font-synthesis: none;
}
* { box-sizing: border-box; }
html { scroll-behavior: smooth; background: var(--cream); }
body { min-width: 320px; margin: 0; color: var(--ink); background: var(--cream); line-height: 1.5; }
a { color: inherit; text-decoration: none; }
h1, h2, h3, h4, p { margin-top: 0; }
h1, h2, h3, h4 { font-family: "Manrope", "Segoe UI", sans-serif; letter-spacing: -.045em; }
.site-header, main, footer { width: min(1320px, calc(100% - 48px)); margin-inline: auto; }
.site-header { position: sticky; top: 0; z-index: 1100; min-height: 68px; display: grid; grid-template-columns: auto 1fr auto; gap: 30px; align-items: center; border-bottom: 1px solid rgba(23,37,32,.2); background: rgba(245,241,232,.92); backdrop-filter: blur(18px); }
.brand { display: flex; align-items: center; gap: 11px; }
.logo-mark { width: 34px; height: 34px; display: grid; place-items: center; border-radius: 50%; background: var(--ink); color: var(--cream); font: 700 11px "Manrope", sans-serif; }
.brand strong { font: 700 16px "Manrope", sans-serif; letter-spacing: -.5px; }
.site-nav { display: flex; justify-content: center; gap: 22px; color: var(--muted); font-size: 11px; }
.site-nav a { padding: 8px 0; border-bottom: 1px solid transparent; }
.site-nav a:hover { color: var(--ink); border-color: var(--orange); }
.header-meta { display: flex; gap: 25px; font-size: 12px; }
.header-meta span + span { color: var(--muted); }

.hero { min-height: 570px; display: grid; grid-template-columns: minmax(0, 1fr) 280px; gap: 70px; align-items: end; padding: 80px 0 64px; border-bottom: 1px solid rgba(23,37,32,.2); }
.hero-kicker { margin: 0 0 24px; color: var(--orange); font-size: 11px; font-weight: 700; letter-spacing: 1.8px; text-transform: uppercase; }
.hero h1 { max-width: 980px; margin: 0; font-size: clamp(58px, 8.5vw, 122px); font-weight: 500; line-height: .86; letter-spacing: -.08em; }
.hero-lede { max-width: 680px; margin: 34px 0 0; color: #676f69; font-size: 18px; line-height: 1.6; }
.hero-meta { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 26px; }
.hero-meta span { padding: 7px 10px; border-radius: 7px; background: #eae5da; color: #56615b; font-size: 10px; }
.trip-profile { display: grid; grid-template-columns: 66px 1fr; align-items: baseline; padding-bottom: 8px; }
.trip-profile > span { grid-column: 1 / -1; margin-bottom: 18px; color: var(--muted); font-size: 10px; letter-spacing: 1.2px; text-transform: uppercase; }
.trip-profile > strong { grid-column: 1 / -1; color: var(--orange); font: 600 32px "Manrope", sans-serif; letter-spacing: -.04em; }
.trip-profile > small { grid-column: 1 / -1; margin: 4px 0 20px; color: var(--muted); }
.trip-profile > div { display: contents; }
.trip-profile b, .trip-profile div small { padding: 12px 0; border-top: 1px solid rgba(23,37,32,.18); }
.trip-profile b { font: 500 25px "Manrope", sans-serif; }
.trip-profile div small { color: #676f69; }

section { scroll-margin-top: 82px; }
section:not(.hero) { padding: 82px 0; border-bottom: 1px solid var(--line); }
.section-head { display: grid; grid-template-columns: minmax(0, 1fr) minmax(260px, 420px); gap: 48px; align-items: end; margin-bottom: 34px; }
.section-head > div { display: flex; gap: 18px; align-items: baseline; }
.section-head span { color: var(--orange); font: italic 15px Georgia, serif; }
.section-head h2 { margin: 0; font-size: clamp(32px, 5vw, 58px); font-weight: 600; line-height: .98; }
.section-head p { margin: 0; color: var(--muted); font-size: 13px; line-height: 1.6; }
.eyebrow { margin-bottom: 13px; color: var(--orange); font-size: 10px; font-weight: 700; letter-spacing: 1px; text-transform: uppercase; }
.price { margin: 14px 0 7px; font: 600 27px "Manrope", sans-serif; letter-spacing: -.045em; }
.button { display: flex; align-items: center; justify-content: space-between; gap: 24px; width: max-content; max-width: 100%; margin-top: 20px; padding: 13px 15px; border: 1px solid var(--ink); color: var(--ink); font-size: 12px; font-weight: 700; }
.button::after { content: "↗"; }
.button:hover { background: var(--ink); color: var(--paper); }
.button-primary { border-color: var(--orange); background: var(--orange); color: white; }
.button-primary:hover { border-color: var(--ink); background: var(--ink); }
.button.is-disabled { border-color: var(--line); color: var(--muted); cursor: not-allowed; opacity: .78; }
.button.is-disabled::after { content: "—"; }
.button.is-disabled:hover { background: transparent; color: var(--muted); }

.budget-grid { display: grid; grid-template-columns: repeat(4, 1fr); border-top: 1px solid rgba(23,37,32,.2); border-left: 1px solid rgba(23,37,32,.2); }
.budget-card { min-height: 310px; padding: 24px; display: flex; flex-direction: column; border-right: 1px solid rgba(23,37,32,.2); border-bottom: 1px solid rgba(23,37,32,.2); background: transparent; }
.budget-card h3 { margin: 0; font-size: 24px; }
.budget-card p { color: var(--muted); font-size: 12px; }
.budget-card .range { margin-bottom: 16px; }
.budget-card .pill { width: max-content; margin-top: auto; padding: 6px 9px; border-radius: 999px; background: #e7efe9; color: #2e6853; font-size: 10px; }
.budget-card:last-child { background: var(--green); color: var(--paper); }
.budget-card:last-child .eyebrow, .budget-card:last-child p { color: rgba(255,255,255,.62); }
.budget-card:last-child .pill { background: rgba(255,255,255,.12); color: var(--paper); }

.transport-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }
.transport-card { padding: 28px; border: 1px solid var(--line); border-radius: 18px; background: var(--paper); box-shadow: 0 16px 40px rgba(57,48,31,.04); }
.transport-overview { display: grid; grid-template-columns: repeat(2, minmax(150px, 1fr)) minmax(220px, auto); gap: 1px; align-items: stretch; margin-bottom: 14px; overflow: hidden; border: 1px solid var(--line); border-radius: 18px; background: var(--line); }
.transport-overview > div { display: grid; gap: 5px; padding: 20px 24px; background: var(--paper); }
.transport-overview span { color: var(--muted); font-size: 10px; letter-spacing: .8px; text-transform: uppercase; }
.transport-overview strong { font: 600 22px "Manrope", sans-serif; letter-spacing: -.04em; }
.transport-overview > .button, .transport-overview > .checkout-note { min-height: 100%; margin: 0; border: 0; border-radius: 0; }
.transport-overview > .button { min-width: 220px; }
.checkout-note { display: grid; place-items: center; padding: 16px 22px; background: var(--paper); color: var(--muted); font-size: 11px; text-align: center; }
.route { display: grid; grid-template-columns: 1fr auto 1fr; gap: 16px; align-items: center; font: 600 24px "Manrope", sans-serif; }
.route span:last-child { text-align: right; }
.route b { width: 42px; height: 42px; display: grid; place-items: center; border-radius: 50%; background: #f0ece4; color: var(--orange); font-size: 17px; }
.times { display: flex; justify-content: space-between; gap: 12px; margin-top: 24px; padding-top: 18px; border-top: 1px solid var(--line); font-size: 12px; }
.transport-card > p { margin: 14px 0 0; color: var(--muted); font-size: 12px; }
.transport-card > small { display: block; margin-top: 4px; color: var(--muted); font-size: 10px; }
.transport-notes, .card-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 16px; }
.transport-notes span, .card-tags span { padding: 6px 8px; border-radius: 999px; background: #eee9df; color: #59635d; font-size: 9px; }

.hotels { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); border-top: 1px solid rgba(23,37,32,.2); border-left: 1px solid rgba(23,37,32,.2); }
.hotel-card, .event-card, .venue-card { min-width: 0; overflow: hidden; border: 1px solid rgba(23,37,32,.18); background: transparent; }
.hotel-card { display: grid; grid-template-rows: auto 1fr; border-top: 0; border-left: 0; }
.hotel-card.selected { background: var(--green); color: var(--paper); }
.hotel-card.selected .card-body p, .hotel-card.selected .hotel-metrics { color: rgba(255,255,255,.62); }
.hotel-card.selected .hotel-metrics { border-color: rgba(255,255,255,.18); }
.hotel-card.selected .button { border-color: var(--orange); background: var(--orange); color: white; }
.hotel-card.selected .button:hover { background: #e94b2d; }
.hotel-tier { position: absolute; top: 15px; right: 15px; z-index: 2; padding: 9px 11px; border-radius: 999px; background: var(--cream); color: var(--ink); font-size: 10px; font-weight: 700; }
.hotel-card .card-body { min-height: 480px; }
.hotel-metrics { display: grid; gap: 7px; margin: 10px 0 18px; padding: 13px 0; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); color: var(--muted); font-size: 10px; }
.hotel-conditions { display: grid; gap: 0; margin: 0 0 18px; padding: 0; list-style: none; }
.hotel-conditions li { display: grid; grid-template-columns: 70px 1fr; gap: 10px; padding: 8px 0; border-bottom: 1px solid var(--line); font-size: 10px; }
.hotel-conditions span { color: var(--muted); }
.hotel-conditions strong { font-weight: 600; }
.hotel-review { margin: 0 0 18px; padding: 14px 15px; border-left: 2px solid var(--orange); background: rgba(255,89,56,.06); color: #59615c; font-size: 11px; font-style: normal; }
.hotel-card.selected .hotel-conditions li { border-color: rgba(255,255,255,.18); }
.hotel-card.selected .hotel-conditions span, .hotel-card.selected .hotel-review { color: rgba(255,255,255,.68); }
.hotel-card.selected .hotel-review { background: rgba(255,255,255,.08); }
.muted-note, .event-address { color: var(--muted) !important; font-size: 11px !important; }
.price small { color: var(--muted); font: 500 10px "DM Sans", sans-serif; letter-spacing: 0; }
.hotel-card.selected .price small { color: rgba(255,255,255,.62); }
.card-grid { display: grid; grid-template-columns: repeat(12, minmax(0, 1fr)); border-top: 1px solid rgba(23,37,32,.2); border-left: 1px solid rgba(23,37,32,.2); }
.event-card, .venue-card { grid-column: span 4; border-top: 0; border-left: 0; }
.card-media { position: relative; }
.card-rank, .card-match { position: absolute; top: 15px; z-index: 2; background: var(--cream); font-size: 10px; font-weight: 700; }
.card-rank { left: 15px; width: 34px; height: 34px; display: grid; place-items: center; border-radius: 50%; }
.card-match { right: 15px; padding: 9px 11px; border-radius: 999px; }
.card-body { min-height: 390px; padding: 25px; display: flex; flex-direction: column; }
.card-body h3 { margin: 4px 0 12px; font-size: clamp(24px, 3vw, 39px); line-height: 1; }
.card-body p { color: #696f6b; font-size: 13px; line-height: 1.55; }
.card-body > small { color: var(--muted); font-size: 10px; }
.card-body .price { margin-top: auto; }
.event-card .card-tags { margin: 0 0 16px; }
.image-frame { position: relative; aspect-ratio: 4 / 3; overflow: hidden; background: #e4ded3; }
.image-frame img { width: 100%; height: 100%; display: block; object-fit: cover; filter: saturate(.88) contrast(1.03); transition: transform .45s ease; }
.hotel-card:hover .image-frame img, .event-card:hover .image-frame img, .venue-card:hover .image-frame img { transform: scale(1.025); }
.image-fallback { display: none; width: 100%; height: 100%; place-items: center; padding: 24px; color: var(--muted); text-align: center; }
.image-frame.failed img { display: none; }
.image-frame.failed .image-fallback, .hotel-card > .image-fallback, .card-media > .image-fallback { display: grid; aspect-ratio: 4 / 3; }
.credit { position: absolute; right: 7px; bottom: 7px; max-width: calc(100% - 14px); padding: 4px 6px; border-radius: 5px; background: rgba(23,37,32,.76); color: white; font-size: 8px; line-height: 1.2; }
.empty { grid-column: 1 / -1; padding: 40px; border: 1px dashed var(--line); color: var(--muted); text-align: center; }

#trip-map { position: relative; min-height: 520px; z-index: 0; overflow: hidden; border: 1px solid var(--line); border-radius: 18px; background: #e6e1d7; }
#trip-map .map-fallback { position: absolute; inset: 0; z-index: 500; display: none; place-items: center; background: #e6e1d7; color: var(--muted); }
#trip-map.failed .map-fallback { display: grid; }
#trip-map.tiles-failed { background: repeating-linear-gradient(45deg,#ece7dd,#ece7dd 12px,#e3ddd2 12px,#e3ddd2 24px); }
.leaflet-popup-content a { font-weight: 700; }
.map-legend { display: flex; flex-wrap: wrap; gap: 17px; margin-top: 13px; color: var(--muted); font-size: 10px; }
.map-legend span::before { content: ""; display: inline-block; width: 7px; height: 7px; margin-right: 6px; border-radius: 50%; background: var(--orange); }
.map-legend .event::before { background: var(--purple); }
.map-legend .restaurant::before { background: var(--gold); }
.map-legend .bar::before { background: var(--green); }

.plans { display: grid; gap: 10px; }
.plan { overflow: hidden; border: 1px solid var(--line); border-radius: 18px; background: var(--paper); }
.plan summary { display: grid; grid-template-columns: 50px 1fr; gap: 16px; padding: 24px; cursor: pointer; list-style: none; }
.plan summary::-webkit-details-marker { display: none; }
.plan summary h3 { margin: 0 0 5px; font-size: 24px; }
.plan summary p { margin: 0; color: var(--muted); font-size: 13px; }
.plan-number { width: 42px; height: 42px; display: grid; place-items: center; border-radius: 50%; background: #f0ece4; color: var(--orange); font: italic 14px Georgia, serif; }
.plan[open] .plan-number { background: var(--orange); color: white; }
.plan-content { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px; padding: 0 24px 28px 90px; }
.plan-day h4 { font-size: 16px; }
.plan-day ol { margin: 0; padding: 0; list-style: none; }
.plan-day li { display: grid; grid-template-columns: 96px 1fr; gap: 12px; padding: 13px 0; border-top: 1px solid var(--line); }
.plan-day time { color: var(--muted); font-size: 11px; font-variant-numeric: tabular-nums; }
.plan-day a { font-size: 13px; font-weight: 700; text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 3px; }
.plan-day p { margin: 4px 0 0; color: var(--muted); font-size: 12px; }

.fine-print { display: grid; grid-template-columns: 1fr 1fr; gap: 48px; color: var(--muted); font-size: 11px; }
.fine-print h2 { margin: 0; color: var(--ink); font-size: 24px; }
.fine-print ul { padding-left: 18px; }
.sources { padding: 0 !important; list-style: none; }
.sources li { display: flex; justify-content: space-between; gap: 24px; padding: 10px 0; border-bottom: 1px solid var(--line); }
.source-link { font-weight: 700; text-decoration: underline; text-underline-offset: 2px; }
.sources small { white-space: nowrap; }
footer { padding: 25px 0 40px; display: flex; justify-content: space-between; color: #777f79; font-size: 10px; letter-spacing: .7px; text-transform: uppercase; }

@media (max-width: 900px) {
  .site-header { grid-template-columns: auto 1fr; }
  .site-nav { justify-content: flex-end; }
  .header-meta { display: none; }
  .hero { min-height: auto; grid-template-columns: 1fr; gap: 48px; }
  .trip-profile { max-width: 320px; }
  .section-head, .fine-print { grid-template-columns: 1fr; }
  .section-head { gap: 14px; }
  .budget-grid { grid-template-columns: repeat(2, 1fr); }
  .hotels { grid-template-columns: 1fr; }
  .transport-overview { grid-template-columns: repeat(2, 1fr); }
  .transport-overview > .button, .transport-overview > .checkout-note { grid-column: 1 / -1; min-height: 58px; }
  .event-card, .venue-card { grid-column: span 6; }
}
@media (max-width: 620px) {
  .site-header, main, footer { width: min(100% - 24px, 1320px); }
  .site-header { min-height: 68px; }
  .site-nav { display: none; }
  .hero { padding: 54px 0 44px; }
  .hero h1 { font-size: 55px; }
  .hero-lede { margin-top: 24px; font-size: 15px; }
  section:not(.hero) { padding: 58px 0; }
  .budget-grid, .transport-grid, .hotels, .transport-overview { grid-template-columns: 1fr; }
  .transport-overview > .button, .transport-overview > .checkout-note { grid-column: auto; }
  .event-card, .venue-card { grid-column: 1 / -1; }
  .card-body { min-height: 350px; padding: 22px 18px; }
  .plan-content { padding-left: 24px; }
  #trip-map { min-height: 380px; }
  footer { flex-direction: column; gap: 8px; }
}
@media print {
  .site-header, main, footer { width: 100%; padding-inline: 20px; }
  .site-header { position: static; }
  .site-nav { display: none; }
  .hero { min-height: auto; padding: 44px 20px; }
  section:not(.hero) { padding: 32px 20px; break-inside: avoid; }
  .button { border: 1px solid var(--ink); color: var(--ink); background: transparent; }
  #trip-map { min-height: 320px; }
}
